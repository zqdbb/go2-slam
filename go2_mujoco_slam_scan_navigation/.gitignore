# ROS 2 / colcon build products
/build/
/install/
/log/
/devel/
/.colcon/
/compile_commands.json

# ROS runtime state
/.ros/
*.bag
*.db3
*.mcap

# Python cache and test output
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# Editor and operating-system files
/.vscode/
/.idea/
/.git.incomplete-*/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Locally copied legacy dependencies. The ROS 2 build uses system GLFW, GLEW,
# OpenGL and GLM packages instead; see README.md for installation commands.
/src/simulator/local_sensing/third_party/
/src/simulator/local_sensing/include/GL/
/src/simulator/local_sensing/include/glad/
/src/simulator/local_sensing/include/glad.c
/src/simulator/local_sensing/include/glm/

# Native build artifacts
*.o
*.obj
*.a
*.so
*.so.*
*.dylib
*.dll

# MuJoCo uses Wavefront OBJ files as source simulation assets, not compiler
# object files. They are required to reproduce the scenes.
!/mujoco/**/*.obj

# Crash dumps and locally copied runtime libraries
/core
/core.*
/libmujoco.so*

# Runtime-generated diagnostics
/frames_*.gv
/frames_*.pdf

# Ad-hoc mapping outputs in the repository root. Curated maps under /maps/
# remain versioned.
/None.data
/None.posegraph
/map.pcd

# Local backup copies made while integrating the RL controller
*.before_rl_*
